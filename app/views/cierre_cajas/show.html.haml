.row.justify-content-center
  .col-md-11
    .card.card-danger.card-outline
      .card-header
        .card-title
          %i.fa.fa-key
          Detalle del Cierre de Caja (Folio #{@cierre_caja.id})
      .card-body
        .row
          .col-md-4
            %h6
              Cajero:
              = @cierre_caja.cajero.nombre
          .col-md-8
            %h6
              Observación:
              = @cierre_caja.observacion
        .row
          .col-md-12
            %p
            %h4 #{@cierre_caja.arqueos.count} #{"arqueo".pluralize(@cierre_caja.arqueos.count).capitalize}
            %table.table.table-hover.table-sm
              %thead.text-right
                %tr
                  %th Folio
                  %th Monto Sistema
                  %th Monto Cajero
                  %th Observación
                  %th Fecha de Creación
                  %th Acción
              %tbody.text-right
                - @arqueos.each do |arqueo|
                  %tr
                    %td= arqueo.id
                    %td= number_to_currency arqueo.monto_sistema
                    %td= number_to_currency arqueo.monto_cajero
                    %td= arqueo.observacion
                    %td= l arqueo.created_at, format: :short
                    %td
                      .btn-group
                        = link_to arqueo_path(arqueo, format: :pdf), class: "btn btn-sm btn-danger", target: "_blank" do
                          %i.fa.fa-print
                          Imprimir
                        = link_to arqueo, class: "btn btn-sm btn-info" do
                          %i.fa.fa-eye
                          Ver
              %tfoot.text-right
                %tr
                  %td{ colspan: 2 }
                    %strong
                      %span{style: "color:green"}
                        = number_to_currency @cierre_caja.monto_sistema
                  %td
                    %strong
                      %span{style: "color:green"}
                        = number_to_currency @cierre_caja.monto_cajero
                  %td{ colspan: 3 }

        .row.justify-content-center
          = paginate @arqueos

        .float-left
          = link_to cierre_caja_path(@cierre_caja, format: :pdf), class: "btn btn-sm btn-danger", target: "_blank" do
            %i.fa.fa-print
            Imprimir
        .btn-group.float-right
          = link_to cierre_cajas_path, class: 'btn btn-sm btn-secondary' do
            %i.fa.fa-arrow-left
            Regresar
          #botonCerrar
            = render "boton_cerrar"
